# Base URL of the deployed read-only Worker API.
# No trailing slash. Leave unset to run the frontend with the pipeline
# disconnected — every data panel will say so rather than invent figures.
VITE_API_BASE_URL=https://matchday-api.gadielanalytics.com
